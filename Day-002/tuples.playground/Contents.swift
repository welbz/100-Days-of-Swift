import UIKit

// Tuples
    // Cant add or remove items from tuple
    // Cant change type of item. Always same type as created with
    // Can access by numericals poistions
    // Can access by names

var name = (first: "Taylor", last: "Swift")
name.0
name.first
name.last

// You can change the value inside tuple, but not the type

