import UIKit

// Sets
    // Items aren't stored in an order
    // Unordered, basically random
    // All items must be unique
    // If you insert Duplicates it will get ignored

let colours = Set(["red", "green", "blue"])

let colours2 = Set(["red", "green", "blue", "red"])

