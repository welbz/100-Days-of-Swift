import UIKit

// Returning values
    // functions send back data


func square(number: Int) -> Int {
    return number * number
}
// returns an Int

let result  = square(number: 8)
print(result)
// setting a let to use the return value of the function
